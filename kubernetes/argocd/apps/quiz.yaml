apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: quiz
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: homelab
  source:
    repoURL: https://github.com/pabpereza/homelab
    targetRevision: HEAD
    path: kubernetes/quiz
  destination:
    server: https://kubernetes.default.svc
    namespace: unir
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
